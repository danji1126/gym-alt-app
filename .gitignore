# Dependencies
node_modules/

# Next.js
.next/
out/

# Build artifacts (재현 가능 — preprocess.ts로 재생성)
public/data/exercises-ko.json

# TypeScript
*.tsbuildinfo
next-env.d.ts

# Environment
.env*.local

# Editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# AI tooling local state (bkit PDCA + Claude Code session) — not for repo
.bkit/
.claude/

# 내부 설계·기획·리포트 문서 — repo 노출 X (로컬 보존)
docs/

# Lockfiles 정책: npm 사용 시 package-lock.json은 커밋
# (다른 패키지 매니저 lockfile은 무시)
yarn.lock
pnpm-lock.yaml
